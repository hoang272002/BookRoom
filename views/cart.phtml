<div class="container">
        <h1>CART</h1>
        <form action="index.php?action=cart" method="POST">
        <input type="hidden" name="formAction" value="addBooking">
            <table>
                <thead>
                    <tr>
                        <th>Room ID</th>
                        <th>Borrow Date</th>
                        <th>Return Date</th>
                        <th>Action</th>
                    </tr>
                </thead>

                <tbody>
                    <?php
    
                    if (isset($_SESSION['cart']) && !empty($_SESSION['cart'])) {
                        foreach ($_SESSION['cart'] as $index => $room) {
                            echo '<tr>';
                            echo '<td>' . htmlspecialchars($room['roomId']) . '</td>';
                            echo '<td>' . htmlspecialchars($room['borrowDate']) . '</td>';
                            echo '<td>' . htmlspecialchars($room['returnDate']) . '</td>';
                            echo '<td><a href="index.php?action=cart&index=' . $index . '" class="remove-button">Remove</a></td>';
                            echo '</tr>';
                        }
                    } else {
                        echo '<tr><td colspan="4">No rooms selected.</td></tr>';
                    }
                    ?>
                </tbody>
            </table>
            <button type="submit" class="borrow-button" name="btnAddbook">Borrow Rooms</button>
        </form>
    </div>

<style>

.container {
    width: 80%;
    margin: 0 auto;
    padding: 20px;
    background-color: #fff;
    box-shadow: 0 0 10px rgba(0,0,0,0.1);
}

h1 {
    color: #333;
    text-align: center;
}

table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 20px;
}

table, th, td {
    border: 1px solid #ddd;
}

th, td {
    padding: 10px;
    text-align: left;
}

th {
    background-color: #f4f4f4;
}

tr:nth-child(even) {
    background-color: #f9f9f9;
}

.remove-button {
    color: #ff0000;
    text-decoration: none;
    font-weight: bold;
}

.remove-button:hover {
    text-decoration: underline;
}

.borrow-button {
    display: block;
    width: 100%;
    padding: 10px;
    background-color: #4CAF50;
    color: #fff;
    border: none;
    font-size: 16px;
    cursor: pointer;
}

.borrow-button:hover {
    background-color: #45a049;
}
    </style>